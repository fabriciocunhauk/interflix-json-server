{
  "name": "interflix-json-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "json-server --watch db.json --port 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fabricio Cunha",
  "license": "ISC",
  "description": "interflix json-server",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
